name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run build
      run: ./jenkins/build-step.sh
    - name: Check Docker composer
      run:  docker-compose ps
